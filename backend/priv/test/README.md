For security reasons, the `.html` files previously located in this directory have been removed because they exposed secrets from the news media providers.

As a result, the tests that depend on these files will currently fail until they are updated.
